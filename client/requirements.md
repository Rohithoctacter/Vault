## Packages
framer-motion | Page transitions and layout animations
date-fns | Formatting dates for notes

## Notes
- Single user auth system implemented via hardcoded credentials in backend.
- API endpoints:
  - POST /api/login (username, password)
  - GET /api/notes
  - POST /api/notes
  - DELETE /api/notes/:id
